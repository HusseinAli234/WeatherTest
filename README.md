## 🚀 Быстрый запуск

### 1️⃣ Подготовка окружения

Создайте файл `.env` в корне проекта и заполните его значениями
(в качестве ориентира используйте `config.py`):

```env
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=mydb
SECRET_KEY=your_very_secret_key
OPENWEATHER_API_KEY=your_api_key_here
```

---

### 2️⃣ Запуск в Docker

Соберите и запустите контейнеры (приложение + две базы данных: **основная** и **тестовая**):

```bash
docker compose up -d --build
```

---

### 3️⃣ Миграции базы данных

Создайте таблицы в базе данных с помощью **Alembic**:

```bash
docker compose exec app alembic upgrade head
```

---

### 4️⃣ Документация API

После запуска приложение будет доступно по следующим адресам:

* 📘 **Swagger UI**: [http://localhost:8000/docs](http://localhost:8000/docs)
* 📕 **ReDoc**: [http://localhost:8000/redoc](http://localhost:8000/redoc)

---

## 🧪 Тестирование

Для запуска тестов используется изолированная база данных `db_test`.
Тесты выполняются **внутри контейнера**:
```bash
docker compose exec app pytest


